export const datalimit = '16kb';

export const options = {
  httpOnly: true, //cookies can't be accessed via browser javascript (document.cookie)
  secure: true,  //only transmitted over secure HTTPS connections
};
